"use client";

// Placeholder export — accept logic is handled server-side in page.tsx via redirect.
// This file exists to allow future client-side enhancement if needed.
export function InviteAcceptButton() {
  return null;
}
